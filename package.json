{
    "files": [
        "src/"
    ],
    "scripts": {
        "start": "parcel serve src/**/*.html",
        "build": "parcel build src/**/*.html --public-url https://mathiasgam.github.io/webgl-examples/",
        "deploy": "gh-pages -d dist"
    },
    "devDependencies": {
        "cross-env": "^5.2.0",
        "gh-pages": "^1.2.0",
        "parcel-bundler": "^1.9.7",
        "typescript": "^2.9.2"
    },
    "dependencies": {
        "@types/gl-matrix": "^3.2.0",
        "gl-matrix": "^3.3.0"
    }
}
